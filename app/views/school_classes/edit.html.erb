<h1>Editing school class</h1>

<%= render "form", url: user_school_class_path(user_id: current_user.id, id: @school_class.id), method: :patch %>

<br>

<div>
  <%= link_to "Back to school classes", user_school_classes_path(current_user) %>
</div>
