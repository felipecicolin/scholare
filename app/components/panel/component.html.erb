<div class="d-flex aligns-items-center justify-content-center">
  <div class="col-md-10 p-3 mt-2">
    <div class="py-5" id="hanging-icons">

      <div class="row g-4 py-5 row-cols-1 row-cols-lg-3">
        <div class="col d-flex align-items-start">
          <div>
            <h3 class="fs-2">
              <%= t("activerecord.models.school_class").pluralize %>
            </h3>
            <p>
              <%= t("panel.descriptions.manage_school_classes") %>
            </p>
            <%= link_to t("panel.manage_school_classes"), school_classes_path, class: "btn btn-primary" %>
          </div>

        </div>
        <div class="col d-flex align-items-start">
          <div>
            <h3 class="fs-2">
              <%= t("activerecord.models.student").pluralize %>
            </h3>
            <p>
              <%= t("panel.descriptions.manage_students") %>
            </p>
            <%= link_to t("panel.manage_students"), students_path, class: "btn btn-primary" %>
          </div>
        </div>

        <div class="col d-flex align-items-start">
          <div>
            <h3 class="fs-2">
              <%= t("activerecord.models.test").pluralize %>
            </h3>
            <p>
              <%= t("panel.descriptions.manage_tests") %>
            </p>
            <%= link_to t("panel.manage_tests"), root_path, class: "btn btn-primary" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
