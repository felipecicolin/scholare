<div class="d-flex aligns-items-center justify-content-center">
  <div class="col-lg-8 p-3 mt-4">
    <h1 class="text-center">
      <%= t("shared.titles.edit", model: t("activerecord.models.student")) %>
    </h1>

    <%= render Form::Component.new(resource: @student,
                                   form_attributes: %i[name],
                                   url: user_student_path(user_id: @current_user.id, id: @student.id)) %>
  </div>
</div>

